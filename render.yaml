services:
  - type: web
    name: lab-ledger
    env: docker
    plan: free
    envVars:
      - key: LAB_DATABASE_URL
        sync: false
      - key: LAB_SECRET_KEY
        generateValue: true
      - key: LAB_ADMIN_PASSWORD
        sync: false
